version: '3.8'

services:
  postgres:
    image: postgres:16.2
    env_file:
      - .env
    ports:
      - '5432:5432'
#    networks:
#      - bootify-service-network
#networks:
#  bootify-service-network:
#    driver: bridge